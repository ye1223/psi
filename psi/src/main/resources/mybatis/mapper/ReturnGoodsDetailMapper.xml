<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roadjava.psi.mapper.ReturnGoodsDetailMapper">

  <select id="selectByRetNo" resultType="com.roadjava.psi.bean.vo.ReturnGoodsDetailVO">
    select
    a.*,
    b.name as goodsName,
    b.unit,
    c.name as supplierName
    from return_goods_detail a
    left join goods b on a.goods_id = b.id
    left join supplier c on a.supplier_id = c.id
    where a.ret_no in
    <foreach item="item" collection="list"  open="(" separator="," close=")">
      #{item}
    </foreach>
  </select>

</mapper>